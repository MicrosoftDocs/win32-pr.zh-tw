---
description: XMU555 所公開的函式。
ms.assetid: afde874c-b4e2-4edb-92bf-895224793e91
title: XMU555 函式
ms.topic: reference
ms.date: 05/31/2018
topic_type:
- APIRef
- kbSyntax
api_name: ''
api_type:
- NA
api_location: ''
ms.openlocfilehash: 4d19da953ccb8b04264f76c7a9ab9d820c7055f4
ms.sourcegitcommit: 9b5faa61c38b2d0c432b7f2dbee8c127b0e28a7e
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 08/19/2021
ms.locfileid: "122475194"
---
# <a name="xmu555-constructors"></a>XMU555 函式

公開的函式 `XMU555` 。

結構 [**XMU555**](/windows/win32/api/directxpackedvector/ns-directxpackedvector-xmu555) 結構的實例。

> [!Note]  
> 此處所列的函式僅適用于 c + +。

 

### <a name="overload-list"></a>多載清單




| 建構函式 | 描述 | 
|-------------|-------------|
| <a href="/windows/desktop/api/directxpackedvector/nf-directxpackedvector-xmu555-xmu555(uint16_t)"><strong>XMU555 (uint16_t) </strong></a> | <code>XMU555</code>從 <code>uint16_t</code> 包含封裝格式之元件資料的變數，初始化的新實例。 <br /> 這個函式會從包含封裝格式之元件資料的變數，初始化 <a href="/windows/desktop/api/directxpackedvector/ns-directxpackedvector-xmu555"><strong>XMU555</strong></a> 的新實例 <code>uint16_t</code> 。 <br /><blockquote>[!Note]<br />這個函式僅適用于 c + +。</blockquote><br /> | 
| <a href="/windows/desktop/api/directxpackedvector/nf-directxpackedvector-xmu555-xmu555(constfloat_bool)"><strong>XMU555 (const float *，bool) </strong></a> | <code>XMU555</code>從三個元素 <code>float</code> 陣列和一個引數，初始化的新實例 <code>bool</code> 。 <br /> 這個函式會從三個元素陣列初始化 <a href="/windows/desktop/api/directxpackedvector/ns-directxpackedvector-xmu555"><strong>XMU555</strong></a> 的新實例 <code>float</code> ， (指定 x-、y 和 z 元件) 和一個 <code>bool</code> (指定 w 元件) 的引數。 <br /><blockquote>[!Note]<br />這個函式僅適用于 c + +。</blockquote><br /> | 
| <a href="/windows/desktop/api/directxpackedvector/nf-directxpackedvector-xmu555-xmu555(constuint8_t_bool)"><strong>XMU555 (const int8_t *，bool) </strong></a> | <code>XMU555</code>從三個元素 <code>int8_t</code> 陣列和一個引數，初始化的新實例 <code>bool</code> 。 <br /> 這個函式會從三個元素陣列初始化 <a href="/windows/desktop/api/directxpackedvector/ns-directxpackedvector-xmu555"><strong>XMU555</strong></a> 的新實例 <code>int8_t</code> ， (指定 x-、y 和 z 元件) 和一個 <code>bool</code> (指定 w 元件) 的引數。 <br /><blockquote>[!Note]<br />這個函式僅適用于 c + +。</blockquote><br /> | 
| <a href="/windows/desktop/api/directxpackedvector/nf-directxpackedvector-xmu555-xmu555(float_float_float_bool)"><strong>XMU555 (float、float、float、bool) </strong></a> | <code>XMU555</code>從三個 <code>float</code> 和一個引數初始化的新實例 <code>bool</code> 。 <br /> 這個函式會從三個 (初始化新的 <a href="/windows/desktop/api/directxpackedvector/ns-directxpackedvector-xmu555"><strong>XMU555</strong></a> 實例， <code>float</code> 其中指定 x-、y 和 z 元件) ，另一個 <code>bool</code> (指定 w 元件) 引數。 <br /><blockquote>[!Note]<br />這個函式僅適用于 c + +。</blockquote><br /> | 
| <a href="/windows/desktop/api/directxpackedvector/nf-directxpackedvector-xmu555-xmu555(float_float_float_bool)"><strong>XMU555 (int8_t、int8_t、int8_t、bool) </strong></a> | <code>XMU555</code>從三個 <code>int8_t</code> 和一個引數初始化的新實例 <code>bool</code> 。 <br /> 這個函式會從三個 (初始化新的 <a href="/windows/desktop/api/directxpackedvector/ns-directxpackedvector-xmu555"><strong>XMU555</strong></a> 實例 <code>int8_t</code> ，其中指定 x-、y 和 z 元件) ，而一個 <code>bool</code> (指定 w 元件) 引數。 <br /><blockquote>[!Note]<br />這個函式僅適用于 c + +。</blockquote><br /> | 
| <a href="/windows/desktop/api/directxpackedvector/nf-directxpackedvector-xmu555-xmu555(constfloat_bool)"><strong>XMU555 () </strong></a> | 的預設函數 <code>XMU555</code><br /> XMU555 的預設函式<a href="/windows/desktop/api/directxpackedvector/ns-directxpackedvector-xmu555"> <strong></strong></a><br /><blockquote>[!Note]<br />這僅適用于以 c + + 為基礎的開發。</blockquote><br /> | 




## <a name="see-also"></a>另請參閱

<dl> <dt>

[XMU555 延伸模組](ovw-xmu555-extensions.md)
</dt> <dt>

**參考**
</dt> <dt>

[**XMU555**](/windows/win32/api/directxpackedvector/ns-directxpackedvector-xmu555)
</dt> </dl>

 

 
