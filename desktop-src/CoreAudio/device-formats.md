---
description: 裝置格式
ms.assetid: 591437e4-21ef-42f1-a752-7f50440cbd63
title: 裝置格式
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: cbcd1b611075191e522cf00d959f120abfb3baa9
ms.sourcegitcommit: c7add10d695482e1ceb72d62b8a4ebd84ea050f7
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 01/07/2021
ms.locfileid: "104110009"
---
# <a name="device-formats"></a><span data-ttu-id="9c7e6-103">裝置格式</span><span class="sxs-lookup"><span data-stu-id="9c7e6-103">Device Formats</span></span>

<span data-ttu-id="9c7e6-104">針對音訊應用程式，使用較高層級音訊 API （例如 DirectSound 或 Windows 多媒體 **waveOutXxx** 函式）的優點是，API 會自動轉換應用程式所使用的資料流程格式，以及音訊裝置所使用的格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-104">For an audio application, a benefit of using a higher-level audio API, such as DirectSound or the Windows multimedia **waveOutXxx** functions, is that the API automatically converts between the stream formats used by the application and the formats used by the audio device.</span></span> <span data-ttu-id="9c7e6-105">相反地，核心音訊 Api 的限制較高，因為它們需要應用程式資料流程使用與裝置所使用的格式相同或密切相關的格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-105">In contrast, the core audio APIs are more restrictive because they require application streams to use formats that are the same as, or are closely related to, the formats used by the device.</span></span> <span data-ttu-id="9c7e6-106">因此，使用核心音訊 Api 來播放或錄製音訊串流的應用程式，可能需要執行資料流程格式之間的部分或所有轉換。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-106">Thus, applications that use the core audio APIs to play or record audio streams might be required to do some or all of the conversions between stream formats.</span></span>

<span data-ttu-id="9c7e6-107">使用 WASAPI 管理共用模式資料流程的應用程式，可以依賴音訊引擎來執行有限的格式轉換。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-107">An application that uses WASAPI to manage shared-mode streams can rely on the audio engine to perform only limited format conversions.</span></span> <span data-ttu-id="9c7e6-108">音訊引擎可以在應用程式所使用的標準 PCM 樣本大小與引擎用於其內部處理的浮點範例之間進行轉換。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-108">The audio engine can convert between a standard PCM sample size used by the application and the floating-point samples that the engine uses for its internal processing.</span></span> <span data-ttu-id="9c7e6-109">不過，應用程式資料流程的格式通常必須有相同數目的通道，以及與裝置使用的資料流程格式相同的取樣率。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-109">However, the format for an application stream typically must have the same number of channels and the same sample rate as the stream format used by the device.</span></span>

<span data-ttu-id="9c7e6-110">如果應用程式使用獨佔模式中的裝置，應用程式必須使用音訊硬體明確支援的串流格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-110">If an application is using a device in exclusive mode, the application must use a stream format that the audio hardware explicitly supports.</span></span> <span data-ttu-id="9c7e6-111">在獨佔模式中，應用程式和裝置會直接交換音訊資料，而不會由音訊引擎介入。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-111">In exclusive mode, the application and device exchange audio data directly, without intervention by the audio engine.</span></span>

<span data-ttu-id="9c7e6-112">許多音訊裝置都支援 PCM 和非 PCM 串流格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-112">Many audio devices support both PCM and non-PCM stream formats.</span></span> <span data-ttu-id="9c7e6-113">不過，音訊引擎只能混合 PCM 串流。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-113">However, the audio engine can mix only PCM streams.</span></span> <span data-ttu-id="9c7e6-114">因此，只有獨佔模式串流可以有非 PCM 格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-114">Thus, only exclusive-mode streams can have non-PCM formats.</span></span> <span data-ttu-id="9c7e6-115">此外，獨佔模式僅支援具有固定資料速率的非 PCM 格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-115">In addition, only non-PCM formats with fixed data rates are supported in exclusive mode.</span></span> <span data-ttu-id="9c7e6-116">固定速率非 PCM 格式的範例是 48-kHz Windows Media 音訊 Professional (WMA Pro) 音訊串流，會在不被解碼的情況下，以數位形式通過索尼/Philips 數位介面 (S/PDIF) 連結。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-116">An example of a fixed-rate non-PCM format is a 48-kHz Windows Media Audio Professional (WMA Pro) audio stream that passes through a Sony/Philips digital interface (S/PDIF) link in digital form without being decoded.</span></span> <span data-ttu-id="9c7e6-117">如需有關透過 S/PDIF 使用 WMA Pro 資料流程的詳細資訊，請參閱下列檔：</span><span class="sxs-lookup"><span data-stu-id="9c7e6-117">For more information about using WMA Pro streams over S/PDIF, see the following documentation:</span></span>

-   <span data-ttu-id="9c7e6-118">\_ \_ \_ Windows DDK 檔中 wave 格式的 WMA SPDIF wave 格式標記的描述。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-118">The description of the WAVE\_FORMAT\_WMA\_SPDIF wave-format tag in the Windows DDK documentation.</span></span>
-   <span data-ttu-id="9c7e6-119">《 Windows 網站的 [音訊裝置技術](https://www.microsoft.com/whdc/device/audio/default.mspx) 》白皮書中的「音訊驅動程式支援 WMA Pro-S/PDIF 格式」白皮書。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-119">The white paper titled "Audio Driver Support for the WMA Pro-over-S/PDIF Format" at the [Audio Device Technologies for Windows](https://www.microsoft.com/whdc/device/audio/default.mspx) website.</span></span>

<span data-ttu-id="9c7e6-120">WASAPI 使用 **WAVEFORMATEX** 或 **WAVEFORMATEXTENSIBLE** 結構來指定資料流程格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-120">WASAPI uses a **WAVEFORMATEX** or **WAVEFORMATEXTENSIBLE** structure to specify a stream format.</span></span> <span data-ttu-id="9c7e6-121">**WAVEFORMATEXTENSIBLE** 結構實際上是一種已擴充的 **WAVEFORMATEX** 結構，可描述更大範圍的格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-121">A **WAVEFORMATEXTENSIBLE** structure is effectively a **WAVEFORMATEX** structure that has been extended to describe a greater range of formats.</span></span> <span data-ttu-id="9c7e6-122">可由獨立 **WAVEFORMATEX** 結構描述的任何格式，也可以透過 **WAVEFORMATEXTENSIBLE** 結構來描述。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-122">Any format that can be described by a stand-alone **WAVEFORMATEX** structure can also be described by a **WAVEFORMATEXTENSIBLE** structure.</span></span>

<span data-ttu-id="9c7e6-123">**WAVEFORMATEXTENSIBLE** 結構的第一個成員是 **WAVEFORMATEX** 結構。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-123">The first member of the **WAVEFORMATEXTENSIBLE** structure is a **WAVEFORMATEX** structure.</span></span> <span data-ttu-id="9c7e6-124">**WAVEFORMATEX** 結構的內容會指出它是獨立的 **WAVEFORMATEX** 結構，還是一部分的 **WAVEFORMATEXTENSIBLE** 結構。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-124">The contents of a **WAVEFORMATEX** structure indicate whether it is a stand-alone **WAVEFORMATEX** structure or part of a **WAVEFORMATEXTENSIBLE** structure.</span></span>

<span data-ttu-id="9c7e6-125">獨立的 **WAVEFORMATEX** 結構可以充分描述具有一或兩個通道的格式，以及一個大小為8位的樣本大小。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-125">A stand-alone **WAVEFORMATEX** structure can adequately describe a format with one or two channels and a sample size that is a multiple of 8 bits.</span></span> <span data-ttu-id="9c7e6-126">**WAVEFORMATEX** 結構本身無法指定通道與喇叭位置的對應。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-126">By itself, a **WAVEFORMATEX** structure cannot specify the mapping of channels to speaker positions.</span></span> <span data-ttu-id="9c7e6-127">此外，雖然 **WAVEFORMATEX** 會指定每個音訊樣本的容器大小，但無法在 (範例中指定有效位數的位數，例如，24位容器中的20位精確度) 。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-127">In addition, although **WAVEFORMATEX** specifies the size of the container for each audio sample, it cannot specify the number of bits of precision in a sample (for example, 20 bits of precision in a 24-bit container).</span></span> <span data-ttu-id="9c7e6-128">相反地， **WAVEFORMATEXTENSIBLE** 結構可以同時指定通道與喇叭的對應，以及每個樣本中的位數位數。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-128">In contrast, the **WAVEFORMATEXTENSIBLE** structure can specify both the mapping of channels to speakers and the number of bits of precision in each sample.</span></span>

<span data-ttu-id="9c7e6-129">如需有關 **WAVEFORMATEX** 和 **WAVEFORMATEXTENSIBLE** 的詳細資訊，請參閱 Windows DDK 檔。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-129">For more information about **WAVEFORMATEX** and **WAVEFORMATEXTENSIBLE**, see the Windows DDK documentation.</span></span>

<span data-ttu-id="9c7e6-130">從 Windows 7 開始， **WAVEFORMATEXTENSIBLE** 已擴充為代表裝置格式，可透過 IEC 61937 相容介面傳輸編碼的音訊。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-130">Starting with Windows 7, the **WAVEFORMATEXTENSIBLE** has been extended to represent device formats for transmitting encoded audio over an IEC 61937-compatible interface.</span></span> <span data-ttu-id="9c7e6-131">如需新結構的詳細資訊，請參閱 [代表 IEC 61937 傳輸的格式](representing-formats-for-iec-61937-transmissions.md)。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-131">For information about the new structure, see [Representing Formats for IEC 61937 Transmissions](representing-formats-for-iec-61937-transmissions.md).</span></span>

### <a name="specifying-the-device-format"></a><span data-ttu-id="9c7e6-132">指定裝置格式</span><span class="sxs-lookup"><span data-stu-id="9c7e6-132">Specifying the Device Format</span></span>

<span data-ttu-id="9c7e6-133">下列 WASAPI 方法會使用 **WAVEFORMATEX** 和 **WAVEFORMATEXTENSIBLE** 結構來描述資料流程格式：</span><span class="sxs-lookup"><span data-stu-id="9c7e6-133">The following WASAPI methods use the **WAVEFORMATEX** and **WAVEFORMATEXTENSIBLE** structures to describe stream formats:</span></span>

-   [<span data-ttu-id="9c7e6-134">**IAudioClient::GetMixFormat**</span><span class="sxs-lookup"><span data-stu-id="9c7e6-134">**IAudioClient::GetMixFormat**</span></span>](/windows/desktop/api/Audioclient/nf-audioclient-iaudioclient-getmixformat)
-   [<span data-ttu-id="9c7e6-135">**IAudioClient::IsFormatSupported**</span><span class="sxs-lookup"><span data-stu-id="9c7e6-135">**IAudioClient::IsFormatSupported**</span></span>](/windows/desktop/api/Audioclient/nf-audioclient-iaudioclient-isformatsupported)
-   [<span data-ttu-id="9c7e6-136">**IAudioClient：： Initialize**</span><span class="sxs-lookup"><span data-stu-id="9c7e6-136">**IAudioClient::Initialize**</span></span>](/windows/desktop/api/Audioclient/nf-audioclient-iaudioclient-initialize)

<span data-ttu-id="9c7e6-137">**GetMixFormat** 方法會抓取音訊引擎用來內部處理共用模式資料流程的資料流程格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-137">The **GetMixFormat** method retrieves the stream format that the audio engine uses for its internal processing of shared-mode streams.</span></span> <span data-ttu-id="9c7e6-138">方法一律會使用 **WAVEFORMATEXTENSIBLE** 結構，而不是使用獨立的 **WAVEFORMATEX** 結構來指定格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-138">The method always uses a **WAVEFORMATEXTENSIBLE** structure, instead of a stand-alone **WAVEFORMATEX** structure, to specify the format.</span></span>

<span data-ttu-id="9c7e6-139">**IsFormatSupported** 方法會指出音訊端點裝置是否支援特定的串流格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-139">The **IsFormatSupported** method indicates whether an audio endpoint device supports a particular stream format.</span></span> <span data-ttu-id="9c7e6-140">呼叫端必須指定資料流程格式是否要在共用模式或獨佔模式中使用。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-140">The caller must specify whether the stream format is intended for use in shared mode or in exclusive mode.</span></span> <span data-ttu-id="9c7e6-141">若是共用模式格式，方法會查詢音訊引擎來判斷它是否支援指定的格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-141">For shared-mode formats, the method queries the audio engine to determine whether it supports the specified format.</span></span> <span data-ttu-id="9c7e6-142">針對獨佔模式格式，方法會查詢設備磁碟機。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-142">For exclusive-mode formats, the method queries the device driver.</span></span> <span data-ttu-id="9c7e6-143">如果格式是由獨立的 **WAVEFORMATEX** 結構所指定，則某些設備磁碟機將會報告其支援1個通道或2通道 PCM 格式，但如果 **WAVEFORMATEXTENSIBLE** 結構指定了格式，則會拒絕相同的格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-143">Some device drivers will report that they support a 1-channel or 2-channel PCM format if the format is specified by a stand-alone **WAVEFORMATEX** structure, but will reject the same format if it is specified by a **WAVEFORMATEXTENSIBLE** structure.</span></span> <span data-ttu-id="9c7e6-144">若要從這些驅動程式取得可靠的結果，獨佔模式應用程式應該針對每個單聲道或2通道 PCM 格式呼叫 **IsFormatSupported** 兩次：一個呼叫應該使用獨立的 **WAVEFORMATEX** 結構來指定格式，而另一個呼叫應該使用 **WAVEFORMATEXTENSIBLE** 結構來指定相同的格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-144">To obtain reliable results from these drivers, exclusive-mode applications should call **IsFormatSupported** twice for each 1-channel or 2-channel PCM format—one call should use a stand-alone **WAVEFORMATEX** structure to specify the format, and the other call should use a **WAVEFORMATEXTENSIBLE** structure to specify the same format.</span></span>

<span data-ttu-id="9c7e6-145">在應用程式使用 **GetMixFormat** 或 **IsFormatSupported** 來尋找適當的共用模式或獨佔模式資料流程格式之後，應用程式就可以呼叫 **initialize** 方法，以使用該格式來初始化資料流程。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-145">After an application has used **GetMixFormat** or **IsFormatSupported** to find an appropriate format for a shared-mode or exclusive-mode stream, the application can call the **Initialize** method to initialize a stream with that format.</span></span> <span data-ttu-id="9c7e6-146">嘗試初始化共用模式資料流程的應用程式，其格式與從 **GetMixFormat** 方法取得的混合格式不同，但具有相同數目的通道和混合格式的取樣速率，可能會成功。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-146">An application that attempts to initialize a shared-mode stream with a format that is not identical to the mix format obtained from the **GetMixFormat** method, but that has the same number of channels and the same sample rate as the mix format, is likely to succeed.</span></span> <span data-ttu-id="9c7e6-147">在呼叫 **initialize** 之前，應用程式可以呼叫 **IsFormatSupported** 來確認 **initialize** 將接受格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-147">Before calling **Initialize**, the application can call **IsFormatSupported** to verify that **Initialize** will accept the format.</span></span>

<span data-ttu-id="9c7e6-148">音訊引擎用來進行共用模式資料流程內部處理的混合格式，與音訊端點裝置在共用模式中使用的資料流程格式不一定相同。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-148">The mix format that the audio engine uses for its internal processing of shared-mode streams is closely related to, but is not necessarily identical to, the stream format that the audio endpoint device uses in shared mode.</span></span> <span data-ttu-id="9c7e6-149">透過 Windows 多媒體控制台 Mmsys.cpl，使用者可以選取音訊端點裝置在共用模式中運作時將使用的串流格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-149">Through the Windows multimedia control panel, Mmsys.cpl, the user can select the stream format that an audio endpoint device will use when it operates in shared mode.</span></span> <span data-ttu-id="9c7e6-150">步驟如下：</span><span class="sxs-lookup"><span data-stu-id="9c7e6-150">The steps are as follows:</span></span>

1.  <span data-ttu-id="9c7e6-151">若要執行 Mmsys.cpl，請開啟命令提示字元視窗，然後輸入下列命令：</span><span class="sxs-lookup"><span data-stu-id="9c7e6-151">To run Mmsys.cpl, open a Command Prompt window and enter the following command:</span></span>

    <span data-ttu-id="9c7e6-152">**控制項 mmsys.cpl**</span><span class="sxs-lookup"><span data-stu-id="9c7e6-152">**control mmsys.cpl**</span></span>

    <span data-ttu-id="9c7e6-153">或者，您也可以用滑鼠右鍵按一下通知區域（位於工作列右邊）中的喇叭圖示，然後選取 [ **播放裝置** ] 或 [ **錄製裝置**]，來執行 Mmsys.cpl。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-153">Alternatively, you can run Mmsys.cpl by right-clicking the speaker icon in the notification area, which is located on the right side of the taskbar, and selecting either **Playback Devices** or **Recording Devices**.</span></span>

2.  <span data-ttu-id="9c7e6-154">Mmsys.cpl 視窗開啟後，從播放裝置清單或錄製裝置清單中選取裝置，然後按一下 [內容 **]。**</span><span class="sxs-lookup"><span data-stu-id="9c7e6-154">After the Mmsys.cpl window opens, select a device from either the list of playback devices or the list of recording devices, and click **Properties**.</span></span>
3.  <span data-ttu-id="9c7e6-155">當 [屬性] 視窗開啟時，按一下 [ **Advanced**]，然後從標示為 [ **預設格式**] 方塊中的可用格式清單中選取格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-155">When the properties window opens, click **Advanced**, and select a format from the list of available formats in the box labeled **Default Format**.</span></span>

<span data-ttu-id="9c7e6-156">例如，假設使用者從播放裝置的可用格式清單中選取下列預設格式：</span><span class="sxs-lookup"><span data-stu-id="9c7e6-156">For example, assume that the user selects the following default format from the list of available formats for a playback device:</span></span>

<span data-ttu-id="9c7e6-157">**2聲道、16位、44100 Hz (CD 品質)**</span><span class="sxs-lookup"><span data-stu-id="9c7e6-157">**2 channel, 16 bit, 44100 Hz (CD Quality)**</span></span>

<span data-ttu-id="9c7e6-158">這是裝置在共用模式中運作時，後續將使用的格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-158">This is the format that the device will subsequently use when it operates in shared mode.</span></span> <span data-ttu-id="9c7e6-159">在 Windows Vista 中，音訊引擎會使用此格式的稍微修改版本，來進行共用模式資料流程的內部處理。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-159">In Windows Vista, the audio engine will use a slightly modified version of this format for its internal processing of shared-mode streams.</span></span> <span data-ttu-id="9c7e6-160">音訊引擎會使用具有相同通道數目的格式 (兩個) 和相同的取樣率 (44100 Hz) ，但它會先將樣本轉換成浮點數，再進行處理。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-160">The audio engine will use a format with the same number of channels (two) and the same sample rate (44100 Hz), but it will convert samples to floating-point numbers before processing them.</span></span> <span data-ttu-id="9c7e6-161">音訊引擎會將輸出混合中的浮點樣本轉換成16位整數，然後才能透過裝置播放這些樣本。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-161">The audio engine will convert the floating-point samples in the output mix to 16-bit integers before playing them through the device.</span></span>

<span data-ttu-id="9c7e6-162">應用程式可以查詢音訊端點裝置的 [**PKEY \_ AudioEngine \_ DeviceFormat**](pkey-audioengine-deviceformat.md) 屬性，以取得使用者為裝置選取的共用模式格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-162">An application can query an audio endpoint device's [**PKEY\_AudioEngine\_DeviceFormat**](pkey-audioengine-deviceformat.md) property to obtain the shared-mode format that the user has selected for the device.</span></span> <span data-ttu-id="9c7e6-163">如需查詢裝置屬性的詳細資訊，請參閱 [裝置屬性](device-properties.md)。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-163">For information about querying the properties of a device, see [Device Properties](device-properties.md).</span></span>

<span data-ttu-id="9c7e6-164">某些應用程式可能會發現裝置的 **PKEY \_ AudioEngine \_ DeviceFormat** 屬性所指定的格式，是在裝置上開啟獨佔模式串流的適當格式。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-164">Some applications might find the format specified by a device's **PKEY\_AudioEngine\_DeviceFormat** property to be a suitable format for opening an exclusive-mode stream on the device.</span></span> <span data-ttu-id="9c7e6-165">其他管理獨佔模式資料流程的應用程式可能會有其他需求，而這些需求會要求與裝置進行複雜的格式協商。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-165">Other applications that manage exclusive-mode streams might have additional requirements that mandate a complex format negotiation with the device.</span></span> <span data-ttu-id="9c7e6-166">一般來說，其中一個應用程式會使用清單開頭的慣用格式來建立適當格式的清單。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-166">Typically, one of these applications constructs a list of suitable formats, with the preferred formats at the beginning of the list.</span></span> <span data-ttu-id="9c7e6-167">然後，應用程式會反復地以清單中的每個後續格式來呼叫 **IsFormatSupported** ，並從清單的開頭開始，直到找到裝置支援的格式為止。</span><span class="sxs-lookup"><span data-stu-id="9c7e6-167">The application then iteratively calls **IsFormatSupported** with each successive format in the list, beginning at the start of the list, until it finds a format that the device supports.</span></span>

## <a name="related-topics"></a><span data-ttu-id="9c7e6-168">相關主題</span><span class="sxs-lookup"><span data-stu-id="9c7e6-168">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="9c7e6-169">音訊端點裝置</span><span class="sxs-lookup"><span data-stu-id="9c7e6-169">Audio Endpoint Devices</span></span>](audio-endpoint-devices.md)
</dt> </dl>

 

 



