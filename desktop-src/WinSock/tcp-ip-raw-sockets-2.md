---
description: 原始通訊端是允許存取基礎傳輸提供者的通訊端類型。
ms.assetid: 4cbe5505-75e7-454f-9e6b-f38bdc60bf6d
title: TCP/IP 原始通訊端
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 25cc08d59d80089a4e655f363e4a899edac8d2b8
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 01/07/2021
ms.locfileid: "106989333"
---
# <a name="tcpip-raw-sockets"></a><span data-ttu-id="24e72-103">TCP/IP 原始通訊端</span><span class="sxs-lookup"><span data-stu-id="24e72-103">TCP/IP Raw Sockets</span></span>

<span data-ttu-id="24e72-104">原始通訊端是允許存取基礎傳輸提供者的通訊端類型。</span><span class="sxs-lookup"><span data-stu-id="24e72-104">A raw socket is a type of socket that allows access to the underlying transport provider.</span></span> <span data-ttu-id="24e72-105">本主題只著重于原始通訊端和 IPv4 與 IPv6 通訊協定。</span><span class="sxs-lookup"><span data-stu-id="24e72-105">This topic focuses only on raw sockets and the IPv4 and IPv6 protocols.</span></span> <span data-ttu-id="24e72-106">這是因為除了 ATM 以外的其他大部分通訊協定不支援原始通訊端。</span><span class="sxs-lookup"><span data-stu-id="24e72-106">This is because most other protocols with the exception of ATM do not support raw sockets.</span></span> <span data-ttu-id="24e72-107">若要使用原始通訊端，應用程式必須有所使用之基礎通訊協定的詳細資訊。</span><span class="sxs-lookup"><span data-stu-id="24e72-107">To use raw sockets, an application needs to have detailed information on the underlying protocol being used.</span></span>

<span data-ttu-id="24e72-108">IP 通訊協定的 Winsock 服務提供者可能會支援 **SOCK \_ RAW** 的通訊端 *類型*。</span><span class="sxs-lookup"><span data-stu-id="24e72-108">Winsock service providers for the IP protocol may support a socket *type* of **SOCK\_RAW**.</span></span> <span data-ttu-id="24e72-109">包含在 Windows 上的 TCP/IP 適用的 Windows 通訊端2提供者支援此 **SOCK \_ 原始** 通訊端類型。</span><span class="sxs-lookup"><span data-stu-id="24e72-109">The Windows Sockets 2 provider for TCP/IP included on Windows supports this **SOCK\_RAW** socket type.</span></span>

<span data-ttu-id="24e72-110">這類原始通訊端有兩種基本類型：</span><span class="sxs-lookup"><span data-stu-id="24e72-110">There are two basic types of such raw sockets:</span></span>

-   <span data-ttu-id="24e72-111">第一個類型會使用由 Winsock 服務提供者所辨識的 IP 標頭中所寫入的已知通訊協定類型。</span><span class="sxs-lookup"><span data-stu-id="24e72-111">The first type uses a known protocol type written in the IP header that is recognized by a Winsock service provider.</span></span> <span data-ttu-id="24e72-112">第一個通訊端類型的範例是 ICMP 通訊協定的通訊端， (IP 通訊協定類型 = 1) 或 ICMPv6 通訊協定 (IP 通訊協定 type = 58) 。</span><span class="sxs-lookup"><span data-stu-id="24e72-112">An example of the first type of socket is a socket for the ICMP protocol (IP protocol type = 1) or the ICMPv6 protocol (IP procotol type = 58).</span></span>
-   <span data-ttu-id="24e72-113">第二種類型可指定任何通訊協定類型。</span><span class="sxs-lookup"><span data-stu-id="24e72-113">The second type allows any protocol type to be specified.</span></span> <span data-ttu-id="24e72-114">第二種類型的範例是，Winsock 服務提供者不直接支援的實驗性通訊協定，例如 Stream Control 傳輸通訊協定 (SCTP) 。</span><span class="sxs-lookup"><span data-stu-id="24e72-114">An example of the second type would be an experimental protocol that is not directly supported by the Winsock service provider such as the Stream Control Transmission Protocol (SCTP).</span></span>

## <a name="determining-if-raw-sockets-are-supported"></a><span data-ttu-id="24e72-115">判斷是否支援原始通訊端</span><span class="sxs-lookup"><span data-stu-id="24e72-115">Determining if Raw Sockets are Supported</span></span>

<span data-ttu-id="24e72-116">如果 Winsock 服務提供者支援 AF INET 或 AF INET6 位址系列的 **SOCK \_ 原始** 通訊端 \_ \_ ，則 **SOCK \_ RAW** 的通訊端類型應該包含在 [**WSAPROTOCOL**](/windows/desktop/api/Winsock2/nf-winsock2-wsaenumprotocolsa)函數針對一或多個可用的傳輸提供者所傳回的 [**WSAEnumProtocols \_ 資訊**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa)結構中。</span><span class="sxs-lookup"><span data-stu-id="24e72-116">If a Winsock service provider supports **SOCK\_RAW** sockets for the AF\_INET or AF\_INET6 address families, the socket type of **SOCK\_RAW** should be included in the [**WSAPROTOCOL\_INFO**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa) structure returned by [**WSAEnumProtocols**](/windows/desktop/api/Winsock2/nf-winsock2-wsaenumprotocolsa) function for one or more of the available transport providers.</span></span>

<span data-ttu-id="24e72-117">[**WSAPROTOCOL \_ 資訊**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa)結構中的 **IADDRESSFAMILY** 成員應指定 af \_ INET 或 af \_ INET6， **iSocketType \_ 資訊** 結構的 **WSAPROTOCOL** 成員應針對其中一個傳輸提供者指定 **SOCK \_ RAW** 。</span><span class="sxs-lookup"><span data-stu-id="24e72-117">The **iAddressFamily** member in the [**WSAPROTOCOL\_INFO**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa) structure should specify AF\_INET or AF\_INET6 and the **iSocketType** member of the **WSAPROTOCOL\_INFO** structure should specify **SOCK\_RAW** for one of the transport providers.</span></span>

<span data-ttu-id="24e72-118">[**WSAPROTOCOL \_ 資訊**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa)結構的 **iProtocol** 成員可以設定為 **IPROTO \_ IP**。</span><span class="sxs-lookup"><span data-stu-id="24e72-118">The **iProtocol** member of the [**WSAPROTOCOL\_INFO**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa) structure may be set to **IPROTO\_IP**.</span></span> <span data-ttu-id="24e72-119">如果服務提供者允許應用程式針對位址系列的網際網路通訊協定以外的其他網路通訊協定使用 **SOCK \_ 原始** 通訊端類型，則 **WSAPROTOCOL \_ 資訊** 結構的 **iProtocol** 成員也可以設定為零。</span><span class="sxs-lookup"><span data-stu-id="24e72-119">The **iProtocol** member of the **WSAPROTOCOL\_INFO** structure may also be set to zero if the service provider allows an application to use a **SOCK\_RAW** socket type for other network protocols other than the Internet Protocol for the address family.</span></span>

<span data-ttu-id="24e72-120">[**WSAPROTOCOL \_ 資訊**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa)結構中的其他成員表示 **SOCK \_ RAW** 之通訊協定支援的其他屬性，並指出應該如何處理 **SOCK \_ 原始** 的通訊端。</span><span class="sxs-lookup"><span data-stu-id="24e72-120">The other members in the [**WSAPROTOCOL\_INFO**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa) structure indicate other properties of the protocol support for **SOCK\_RAW** and indicate how a socket of **SOCK\_RAW** should be treated.</span></span> <span data-ttu-id="24e72-121">**SOCK \_ 原始** 的其他 **WSAPROTOCOL \_ 資訊** 成員通常會指定通訊協定為無訊息導向、支援廣播/多播 (XP1 \_ 無連接、XP1 \_ 訊息 \_ 導向、XP1 \_ 支援 \_ 廣播，以及 XP1 \_ \_ 成員) 中設定 dwServiceFlags1 支援 MULTIPOINT 位，且最大訊息大小為65467個位元組。</span><span class="sxs-lookup"><span data-stu-id="24e72-121">These other members of the **WSAPROTOCOL\_INFO** for **SOCK\_RAW** normally specify that the protocol is connectionless, message-oriented, supports broadcast/multicast (the XP1\_CONNECTIONLESS, XP1\_MESSAGE\_ORIENTED, XP1\_SUPPORT\_BROADCAST, and XP1\_SUPPORT\_MULTIPOINT bits are set in the dwServiceFlags1 member), and can have a maximum message size of 65,467 bytes.</span></span>

<span data-ttu-id="24e72-122">在 Windows XP 和更新版本上， *NetSh.exe* 命令可用於判斷是否支援原始通訊端。</span><span class="sxs-lookup"><span data-stu-id="24e72-122">On Windows XP and later, the *NetSh.exe* command can be used to determine if raw sockets are supported.</span></span> <span data-ttu-id="24e72-123">下列從 CMD 視窗執行的命令會在主控台上顯示 Winsock 目錄中的資料：</span><span class="sxs-lookup"><span data-stu-id="24e72-123">The following command run from a CMD window will display data from the Winsock catalog on the console:</span></span>

<span data-ttu-id="24e72-124">**netsh winsock show catalog**</span><span class="sxs-lookup"><span data-stu-id="24e72-124">**netsh winsock show catalog**</span></span>

<span data-ttu-id="24e72-125">輸出會包含一份清單，其中包含本機電腦上所支援之 [**WSAPROTOCOL \_ 資訊**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa) 結構中的部分資料。</span><span class="sxs-lookup"><span data-stu-id="24e72-125">The output will include a list that contains some of the data from the [**WSAPROTOCOL\_INFO**](/windows/win32/api/winsock2/ns-winsock2-wsaprotocol_infoa) structures supported on the local computer.</span></span> <span data-ttu-id="24e72-126">在 [描述] 欄位中搜尋 RAW/IP 或 RAW/IPv6 一詞，以找出支援原始通訊端的通訊協定。</span><span class="sxs-lookup"><span data-stu-id="24e72-126">Search for the term RAW/IP or RAW/IPv6 in the Description field to find those protocols that support raw sockets.</span></span>

## <a name="creating-a-raw-socket"></a><span data-ttu-id="24e72-127">建立原始通訊端</span><span class="sxs-lookup"><span data-stu-id="24e72-127">Creating a Raw Socket</span></span>

<span data-ttu-id="24e72-128">若要建立 **SOCK \_ RAW** 型別的通訊端，請使用 *af* 參數呼叫 [**socket**](/windows/desktop/api/Winsock2/nf-winsock2-socket)或 [**WSASocket**](/windows/desktop/api/Winsock2/nf-winsock2-wsasocketa)函式 (位址家族) 設定為 af \_ INET 或 af \_ INET6、將 *type* 參數設定為 **SOCK \_ RAW**，以及將 *protocol* 參數設定為所需的通訊協定號碼。</span><span class="sxs-lookup"><span data-stu-id="24e72-128">To create a socket of type **SOCK\_RAW**, call the [**socket**](/windows/desktop/api/Winsock2/nf-winsock2-socket) or [**WSASocket**](/windows/desktop/api/Winsock2/nf-winsock2-wsasocketa) function with the *af* parameter (address family) set to AF\_INET or AF\_INET6, the *type* parameter set to **SOCK\_RAW**, and the *protocol* parameter set to the protocol number required.</span></span> <span data-ttu-id="24e72-129">*通訊協定* 參數會成為 IP 標頭中的通訊協定值， (SCTP 是132，例如) 。</span><span class="sxs-lookup"><span data-stu-id="24e72-129">The *protocol* parameter becomes the protocol value in the IP header (SCTP is 132, for example).</span></span>

> [!Note]  
> <span data-ttu-id="24e72-130">如果 *類型* 參數設定為 **SOCK \_ RAW**，應用程式可能不會指定零 (0) 作為 [**socket**](/windows/desktop/api/Winsock2/nf-winsock2-socket)、 [**WSASocket**](/windows/desktop/api/Winsock2/nf-winsock2-wsasocketa)和 [**WSPSocket**](/windows/desktop/api/Ws2spi/nc-ws2spi-lpwspsocket)函數的 *通訊協定* 參數。</span><span class="sxs-lookup"><span data-stu-id="24e72-130">An application may not specify zero (0) as the *protocol* parameter for the [**socket**](/windows/desktop/api/Winsock2/nf-winsock2-socket), [**WSASocket**](/windows/desktop/api/Winsock2/nf-winsock2-wsasocketa), and [**WSPSocket**](/windows/desktop/api/Ws2spi/nc-ws2spi-lpwspsocket) functions if the *type* parameter is set to **SOCK\_RAW**.</span></span>

 

<span data-ttu-id="24e72-131">原始通訊端提供操作基礎傳輸的功能，因此可用於造成安全性威脅的惡意用途。</span><span class="sxs-lookup"><span data-stu-id="24e72-131">Raw sockets offer the capability to manipulate the underlying transport, so they can be used for malicious purposes that pose a security threat.</span></span> <span data-ttu-id="24e72-132">因此，只有 Administrators 群組的成員可以 \_ 在 Windows 2000 和更新版本上建立 SOCK RAW 類型的通訊端。</span><span class="sxs-lookup"><span data-stu-id="24e72-132">Therefore, only members of the Administrators group can create sockets of type SOCK\_RAW on Windows 2000 and later.</span></span>

## <a name="send-and-receive-operations"></a><span data-ttu-id="24e72-133">傳送和接收作業</span><span class="sxs-lookup"><span data-stu-id="24e72-133">Send and Receive Operations</span></span>

<span data-ttu-id="24e72-134">一旦應用程式建立 **SOCK \_ RAW** 型別的通訊端，就可以使用這個通訊端來傳送和接收資料。</span><span class="sxs-lookup"><span data-stu-id="24e72-134">Once an application creates a socket of type **SOCK\_RAW**, this socket may be used to send and receive data.</span></span> <span data-ttu-id="24e72-135">在 **SOCK \_ RAW** 類型的通訊端上傳送或接收的所有封包，都會被視為未連接之通訊端上的資料包。</span><span class="sxs-lookup"><span data-stu-id="24e72-135">All packets sent or received on a socket of type **SOCK\_RAW** are treated as datagrams on an unconnected socket.</span></span>

<span data-ttu-id="24e72-136">下列規則適用于透過 **SOCK \_ 原始** 通訊端的作業：</span><span class="sxs-lookup"><span data-stu-id="24e72-136">The following rules apply to the operations over **SOCK\_RAW** sockets:</span></span>

-   <span data-ttu-id="24e72-137">[**Sendto**](/windows/desktop/api/winsock/nf-winsock-sendto)或 [**WSASendTo**](/windows/desktop/api/Winsock2/nf-winsock2-wsasendto)函式通常用來在 **SOCK \_ RAW** 類型的通訊端上傳送資料。</span><span class="sxs-lookup"><span data-stu-id="24e72-137">The [**sendto**](/windows/desktop/api/winsock/nf-winsock-sendto) or [**WSASendTo**](/windows/desktop/api/Winsock2/nf-winsock2-wsasendto) function is normally used to send data on a socket of type **SOCK\_RAW**.</span></span> <span data-ttu-id="24e72-138">目的地位址可以是通訊端位址家族中的任何有效位址，包括廣播或多播位址。</span><span class="sxs-lookup"><span data-stu-id="24e72-138">The destination address can be any valid address in the socket's address family, including a broadcast or multicast address.</span></span> <span data-ttu-id="24e72-139">若要傳送至廣播位址，應用程式必須已使用 [**setsockopt**](/windows/desktop/api/winsock/nf-winsock-setsockopt) 並 \_ 啟用廣播。</span><span class="sxs-lookup"><span data-stu-id="24e72-139">To send to a broadcast address, an application must have used [**setsockopt**](/windows/desktop/api/winsock/nf-winsock-setsockopt) with SO\_BROADCAST enabled.</span></span> <span data-ttu-id="24e72-140">否則， **sendto** 或 **WSASendTo** 將會失敗，並出現錯誤碼 [WSAEACCES](windows-sockets-error-codes-2.md)。</span><span class="sxs-lookup"><span data-stu-id="24e72-140">Otherwise, **sendto** or **WSASendTo** will fail with the error code [WSAEACCES](windows-sockets-error-codes-2.md).</span></span> <span data-ttu-id="24e72-141">若為 IP，應用程式可以傳送至任何多播位址 (而不會成為群組成員) 。</span><span class="sxs-lookup"><span data-stu-id="24e72-141">For IP, an application can send to any multicast address (without becoming a group member).</span></span>
-   <span data-ttu-id="24e72-142">傳送 IPv4 資料時，應用程式可選擇是否要在封包的傳出資料包前端指定 IPv4 標頭。</span><span class="sxs-lookup"><span data-stu-id="24e72-142">When sending IPv4 data, an application has a choice on whether to specify the IPv4 header at the front of the outgoing datagram for the packet.</span></span> <span data-ttu-id="24e72-143">如果 IPv4 通訊端的 **IP \_ HDRINCL** 通訊端選項設定為 true （ (位址系列為 AF \_ INET) ），則應用程式必須在傳出的資料中提供傳送作業的 IPv4 標頭。</span><span class="sxs-lookup"><span data-stu-id="24e72-143">If the **IP\_HDRINCL** socket option is set to true for an IPv4 socket (address family of AF\_INET), the application must supply the IPv4 header in the outgoing data for send operations.</span></span> <span data-ttu-id="24e72-144">如果這個通訊端選項是 false (預設設定) ，則 IPv4 標頭不應該包含在傳送作業的傳出資料中。</span><span class="sxs-lookup"><span data-stu-id="24e72-144">If this socket option is false (the default setting), then the IPv4 header should not be in included the outgoing data for send operations.</span></span>
-   <span data-ttu-id="24e72-145">傳送 IPv6 資料時，應用程式可選擇是否要在封包的傳出資料包前端指定 IPv6 標頭。</span><span class="sxs-lookup"><span data-stu-id="24e72-145">When sending IPv6 data, an application has a choice on whether to specify the IPv6 header at the front of the outgoing datagram for the packet.</span></span> <span data-ttu-id="24e72-146">如果 ipv6 通訊端的 **ipv6 \_ HDRINCL** 通訊端選項設定為 true (位址系列為 AF \_ INET6) ，則應用程式必須在傳出的資料中提供 ipv6 標頭，以進行傳送作業。</span><span class="sxs-lookup"><span data-stu-id="24e72-146">If the **IPV6\_HDRINCL** socket option is set to true for an IPv6 socket (address family of AF\_INET6), the application must supply the IPv6 header in the outgoing data for send operations.</span></span> <span data-ttu-id="24e72-147">此選項的預設設定為 [false]。</span><span class="sxs-lookup"><span data-stu-id="24e72-147">The default setting for this option is false.</span></span> <span data-ttu-id="24e72-148">如果這個通訊端選項為 false (預設設定) ，則不應將 IPv6 標頭包含在傳送作業的傳出資料中。</span><span class="sxs-lookup"><span data-stu-id="24e72-148">If this socket option is false (the default setting), then the IPv6 header should not be included in the outgoing data for send operations.</span></span> <span data-ttu-id="24e72-149">若為 IPv6，則應該不需要包含 IPv6 標頭。</span><span class="sxs-lookup"><span data-stu-id="24e72-149">For IPv6, there should be no need to include the IPv6 header.</span></span> <span data-ttu-id="24e72-150">如果可以使用通訊端函式提供資訊，則不應納入 IPv6 標頭，以避免未來發生相容性問題。</span><span class="sxs-lookup"><span data-stu-id="24e72-150">If information is available using socket functions, then the IPv6 header should not be included to avoid compatibility problems in the future.</span></span> <span data-ttu-id="24e72-151">這些問題會在 IETF 發行的 RFC 3542 中討論。</span><span class="sxs-lookup"><span data-stu-id="24e72-151">These issues are discussed in RFC 3542 published by the IETF.</span></span> <span data-ttu-id="24e72-152">不建議使用 **IPV6 \_ HDRINCL** 通訊端選項，而且未來可能會淘汰。</span><span class="sxs-lookup"><span data-stu-id="24e72-152">Using the **IPV6\_HDRINCL** socket option is not recommended and may be deprecated in future.</span></span>
-   <span data-ttu-id="24e72-153">[**Recvfrom**](/windows/desktop/api/winsock/nf-winsock-recvfrom)或 [**WSARecvFrom**](/windows/desktop/api/Winsock2/nf-winsock2-wsarecvfrom)函數通常用來接收 **SOCK \_ RAW** 類型之通訊端上的資料。</span><span class="sxs-lookup"><span data-stu-id="24e72-153">The [**recvfrom**](/windows/desktop/api/winsock/nf-winsock-recvfrom) or [**WSARecvFrom**](/windows/desktop/api/Winsock2/nf-winsock2-wsarecvfrom) function is normally used to receive data on a socket of type **SOCK\_RAW**.</span></span> <span data-ttu-id="24e72-154">這兩個函式都有一個選項，可傳回傳送封包的來源 IP 位址。</span><span class="sxs-lookup"><span data-stu-id="24e72-154">Both of these functions have an option to return the source IP address where the packet was sent from.</span></span> <span data-ttu-id="24e72-155">收到的資料是來自未連接之通訊端的資料包。</span><span class="sxs-lookup"><span data-stu-id="24e72-155">The received data is a datagram from an unconnected socket.</span></span>
-   <span data-ttu-id="24e72-156">針對 IPv4 (位址系列的 AF \_ INET) ，應用程式會在每個收到的資料包前端收到 ip 標頭，不論 **ip \_ HDRINCL** 通訊端選項為何。</span><span class="sxs-lookup"><span data-stu-id="24e72-156">For IPv4 (address family of AF\_INET), an application receives the IP header at the front of each received datagram regardless of the **IP\_HDRINCL** socket option.</span></span>
-   <span data-ttu-id="24e72-157">針對 IPv6 (位址系列的 AF \_ INET6) ，應用程式會在每個收到的資料包中的最後一個 IPv6 標頭之後收到所有內容，不論 **IPv6 \_ HDRINCL** 通訊端選項為何。</span><span class="sxs-lookup"><span data-stu-id="24e72-157">For IPv6 (address family of AF\_INET6), an application receives everything after the last IPv6 header in each received datagram regardless of the **IPV6\_HDRINCL** socket option.</span></span> <span data-ttu-id="24e72-158">應用程式不會使用原始通訊端接收任何 IPv6 標頭。</span><span class="sxs-lookup"><span data-stu-id="24e72-158">The application does not receive any IPv6 headers using a raw socket.</span></span>
-   <span data-ttu-id="24e72-159">收到的資料包會複製到所有符合下列條件的 **SOCK \_ 原始** 通訊端：</span><span class="sxs-lookup"><span data-stu-id="24e72-159">Received datagrams are copied into all **SOCK\_RAW** sockets that satisfy the following conditions:</span></span>

    -   <span data-ttu-id="24e72-160">當通訊端建立時， *protocol* 參數中所指定的通訊協定號碼應符合所接收資料包的 IP 標頭中的通訊協定號碼。</span><span class="sxs-lookup"><span data-stu-id="24e72-160">The protocol number specified in the *protocol* parameter when the socket was created should match the protocol number in the IP header of the received datagram.</span></span>
    -   <span data-ttu-id="24e72-161">如果為通訊端定義了本機 IP 位址，則應該對應至接收資料包的 IP 標頭中所指定的目的地位址。</span><span class="sxs-lookup"><span data-stu-id="24e72-161">If a local IP address is defined for the socket, it should correspond to the destination address as specified in the IP header of the received datagram.</span></span> <span data-ttu-id="24e72-162">應用程式可以藉由呼叫 [**bind**](/windows/desktop/api/winsock/nf-winsock-bind) 函式來指定本機 IP 位址。</span><span class="sxs-lookup"><span data-stu-id="24e72-162">An application may specify the local IP address by calling the [**bind**](/windows/desktop/api/winsock/nf-winsock-bind) function.</span></span> <span data-ttu-id="24e72-163">如果未指定通訊端的本機 IP 位址，則不論收到的資料包 IP 標頭中的目的地 IP 位址為何，資料包都會複製到通訊端中。</span><span class="sxs-lookup"><span data-stu-id="24e72-163">If no local IP address is specified for the socket, the datagrams are copied into the socket regardless of the destination IP address in the IP header of the received datagram.</span></span>
    -   <span data-ttu-id="24e72-164">如果為通訊端定義了外部地址，則應該對應至接收資料包的 IP 標頭中所指定的來源位址。</span><span class="sxs-lookup"><span data-stu-id="24e72-164">If a foreign address is defined for the socket, it should correspond to the source address as specified in the IP header of the received datagram.</span></span> <span data-ttu-id="24e72-165">應用程式可以藉由呼叫 [**connect**](/windows/desktop/api/Winsock2/nf-winsock2-connect) 或 [**WSAConnect**](/windows/desktop/api/Winsock2/nf-winsock2-wsaconnect) 函數來指定外部 IP 位址。</span><span class="sxs-lookup"><span data-stu-id="24e72-165">An application may specify the foreign IP address by calling the [**connect**](/windows/desktop/api/Winsock2/nf-winsock2-connect) or [**WSAConnect**](/windows/desktop/api/Winsock2/nf-winsock2-wsaconnect) function.</span></span> <span data-ttu-id="24e72-166">如果未指定通訊端的外部 IP 位址，則不論收到的資料包 IP 標頭中的來源 IP 位址為何，資料包都會複製到通訊端中。</span><span class="sxs-lookup"><span data-stu-id="24e72-166">If no foreign IP address is specified for the socket, the datagrams are copied into the socket regardless of the source IP address in the IP header of the received datagram.</span></span>

<span data-ttu-id="24e72-167">請務必瞭解， **SOCK \_ RAW** 類型的某些通訊端可能會收到許多未預期的資料包。</span><span class="sxs-lookup"><span data-stu-id="24e72-167">It is important to understand that some sockets of type **SOCK\_RAW** may receive many unexpected datagrams.</span></span> <span data-ttu-id="24e72-168">例如，偵測程式可能會建立 **SOCK \_ RAW** 類型的通訊端，以傳送 ICMP echo 要求和接收回應。</span><span class="sxs-lookup"><span data-stu-id="24e72-168">For example, a PING program may create a socket of type **SOCK\_RAW** to send ICMP echo requests and receive responses.</span></span> <span data-ttu-id="24e72-169">雖然應用程式預期會有 ICMP echo 回應，但其他所有的 ICMP 訊息 (例如，ICMP 主機 \_ 無法連線) 也可以傳遞到此應用程式。</span><span class="sxs-lookup"><span data-stu-id="24e72-169">While the application is expecting ICMP echo responses, all other ICMP messages (such as ICMP HOST\_UNREACHABLE) may also be delivered to this application.</span></span> <span data-ttu-id="24e72-170">此外，如果在電腦上同時開啟數個 **SOCK \_ 原始** 通訊端，則可以將相同的資料包傳遞給所有開啟的通訊端。</span><span class="sxs-lookup"><span data-stu-id="24e72-170">Moreover, if several **SOCK\_RAW** sockets are open on a computer at the same time, the same datagrams may be delivered to all the open sockets.</span></span> <span data-ttu-id="24e72-171">應用程式必須有一種機制來辨識感興趣的資料包，並忽略所有其他資料包。</span><span class="sxs-lookup"><span data-stu-id="24e72-171">An application must have a mechanism to recognize the datagrams of interest and to ignore all others.</span></span> <span data-ttu-id="24e72-172">針對 PING 程式，這類機制可能包括檢查 ICMP 標頭中唯一識別碼的已接收 IP 標頭 (應用程式的處理序識別碼，例如) 。</span><span class="sxs-lookup"><span data-stu-id="24e72-172">For a PING program, such a mechanism might include inspecting the received IP header for unique identifiers in the ICMP header (the application's process ID, for example).</span></span>

> [!Note]  
> <span data-ttu-id="24e72-173">若要使用 **SOCK \_ RAW** 型別的通訊端，需要系統管理許可權。</span><span class="sxs-lookup"><span data-stu-id="24e72-173">To use a socket of type **SOCK\_RAW** requires administrative privileges.</span></span> <span data-ttu-id="24e72-174">執行使用原始通訊端之 Winsock 應用程式的使用者必須是本機電腦上 Administrators 群組的成員，否則原始通訊端呼叫會失敗，並出現錯誤碼 [WSAEACCES](windows-sockets-error-codes-2.md)。</span><span class="sxs-lookup"><span data-stu-id="24e72-174">Users running Winsock applications that use raw sockets must be a member of the Administrators group on the local computer, otherwise raw socket calls will fail with an error code of [WSAEACCES](windows-sockets-error-codes-2.md).</span></span> <span data-ttu-id="24e72-175">在 Windows Vista 和更新版本上，會在建立通訊端時強制執行原始通訊端的存取。</span><span class="sxs-lookup"><span data-stu-id="24e72-175">On Windows Vista and later, access for raw sockets is enforced at socket creation.</span></span> <span data-ttu-id="24e72-176">在舊版的 Windows 中，會在其他通訊端作業期間強制執行原始通訊端的存取。</span><span class="sxs-lookup"><span data-stu-id="24e72-176">In earlier versions of Windows, access for raw sockets is enforced during other socket operations.</span></span>

 

## <a name="common-uses-of-raw-sockets"></a><span data-ttu-id="24e72-177">原始通訊端的常見用法</span><span class="sxs-lookup"><span data-stu-id="24e72-177">Common Uses of Raw Sockets</span></span>

<span data-ttu-id="24e72-178">原始通訊端的常見用法之一，是針對需要詳細檢查 IP 封包和標頭的應用程式進行疑難排解。</span><span class="sxs-lookup"><span data-stu-id="24e72-178">One common use of raw sockets are troubleshooting applications that need to examine IP packets and headers in detail.</span></span> <span data-ttu-id="24e72-179">例如，原始通訊端可以搭配 SIO RCVALL IOCTL 使用， \_ 以便讓通訊端接收透過網路介面傳遞的所有 IPv4 或 IPv6 封包。</span><span class="sxs-lookup"><span data-stu-id="24e72-179">For example, a raw socket can be used with the SIO\_RCVALL IOCTL to enable a socket to receive all IPv4 or IPv6 packets passing through a network interface.</span></span> <span data-ttu-id="24e72-180">如需詳細資訊，請參閱 [**SIO \_ RCVALL**](/previous-versions/windows/desktop/legacy/ee309610(v=vs.85)) 參考。</span><span class="sxs-lookup"><span data-stu-id="24e72-180">For more information, see the [**SIO\_RCVALL**](/previous-versions/windows/desktop/legacy/ee309610(v=vs.85)) reference.</span></span>

## <a name="limitations-on-raw-sockets"></a><span data-ttu-id="24e72-181">原始通訊端的限制</span><span class="sxs-lookup"><span data-stu-id="24e72-181">Limitations on Raw Sockets</span></span>

<span data-ttu-id="24e72-182">在 Windows 7、Windows Vista、Windows XP （含 Service Pack 2） (SP2) ，以及 Windows XP Service Pack 3 (SP3) 上，透過原始通訊端傳送流量的功能有數種限制：</span><span class="sxs-lookup"><span data-stu-id="24e72-182">On Windows 7, Windows Vista, Windows XP with Service Pack 2 (SP2), and Windows XP with Service Pack 3 (SP3), the ability to send traffic over raw sockets has been restricted in several ways:</span></span>

-   <span data-ttu-id="24e72-183">無法透過原始通訊端傳送 TCP 資料。</span><span class="sxs-lookup"><span data-stu-id="24e72-183">TCP data cannot be sent over raw sockets.</span></span>
-   <span data-ttu-id="24e72-184">具有無效來源位址的 UDP 資料包無法透過原始通訊端傳送。</span><span class="sxs-lookup"><span data-stu-id="24e72-184">UDP datagrams with an invalid source address cannot be sent over raw sockets.</span></span> <span data-ttu-id="24e72-185">任何外寄 UDP 資料包的 IP 來源位址都必須存在於網路介面上，否則資料包會卸載。</span><span class="sxs-lookup"><span data-stu-id="24e72-185">The IP source address for any outgoing UDP datagram must exist on a network interface or the datagram is dropped.</span></span> <span data-ttu-id="24e72-186">這項變更是為了限制惡意程式碼建立分散式阻絕服務攻擊的能力，並限制傳送詐騙封包 (具有偽造來源 IP 位址) 的 TCP/IP 封包的能力。</span><span class="sxs-lookup"><span data-stu-id="24e72-186">This change was made to limit the ability of malicious code to create distributed denial-of-service attacks and limits the ability to send spoofed packets (TCP/IP packets with a forged source IP address).</span></span>
-   <span data-ttu-id="24e72-187">不允許使用 IPPROTO TCP 通訊協定的原始通訊端呼叫 [**bind**](/windows/desktop/api/winsock/nf-winsock-bind) 函數 \_ 。</span><span class="sxs-lookup"><span data-stu-id="24e72-187">A call to the [**bind**](/windows/desktop/api/winsock/nf-winsock-bind) function with a raw socket for the IPPROTO\_TCP protocol is not allowed.</span></span>
    > [!Note]  
    > <span data-ttu-id="24e72-188">具有原始套 [**接字的系**](/windows/desktop/api/winsock/nf-winsock-bind) 結函式可用於其他通訊協定 (IPPROTO \_ IP、IPPROTO \_ UDP 或 IPPROTO \_ SCTP，例如) 。</span><span class="sxs-lookup"><span data-stu-id="24e72-188">The [**bind**](/windows/desktop/api/winsock/nf-winsock-bind) function with a raw socket is allowed for other protocols (IPPROTO\_IP, IPPROTO\_UDP, or IPPROTO\_SCTP, for example).</span></span>

     

<span data-ttu-id="24e72-189">上述的限制不適用於 Windows Server 2008 R2、Windows Server 2008、Windows Server 2003，或 Windows XP SP2 之前的作業系統版本。</span><span class="sxs-lookup"><span data-stu-id="24e72-189">These above restrictions do not apply to Windows Server 2008 R2, Windows Server 2008 , Windows Server 2003, or to versions of the operating system earlier than Windows XP with SP2.</span></span>

> [!Note]  
> <span data-ttu-id="24e72-190">Microsoft 在 Windows 上執行 TCP/IP 的功能，能夠根據上述限制開啟原始的 UDP 或 TCP 通訊端。</span><span class="sxs-lookup"><span data-stu-id="24e72-190">The Microsoft implementation of TCP/IP on Windows is capable of opening a raw UDP or TCP socket based on the above restrictions.</span></span> <span data-ttu-id="24e72-191">其他 Winsock 提供者可能不支援使用原始通訊端。</span><span class="sxs-lookup"><span data-stu-id="24e72-191">Other Winsock providers may not support the use of raw sockets.</span></span>

 

<span data-ttu-id="24e72-192">使用 **SOCK \_ RAW** 類型之通訊端的應用程式會有更多的限制。</span><span class="sxs-lookup"><span data-stu-id="24e72-192">There are further limitations for applications that use a socket of type **SOCK\_RAW**.</span></span> <span data-ttu-id="24e72-193">例如，所有接聽特定通訊協定的應用程式都會收到針對此通訊協定接收的所有封包。</span><span class="sxs-lookup"><span data-stu-id="24e72-193">For example, all applications listening for a specific protocol will receive all packets received for this protocol.</span></span> <span data-ttu-id="24e72-194">這可能不是使用通訊協定的多個應用程式所需的內容。</span><span class="sxs-lookup"><span data-stu-id="24e72-194">This may not be what is desired for multiple applications using a protocol.</span></span> <span data-ttu-id="24e72-195">這也不適合高效能應用程式。</span><span class="sxs-lookup"><span data-stu-id="24e72-195">This is also not suitable for high-performance applications.</span></span> <span data-ttu-id="24e72-196">若要解決這些問題，可能需要為特定的網路通訊協定撰寫 Windows 網路通訊協定驅動程式 (設備磁碟機) 。</span><span class="sxs-lookup"><span data-stu-id="24e72-196">To get around these issues, it may be required to write a Windows network protocol driver (device driver) for the specific network protocol.</span></span> <span data-ttu-id="24e72-197">在 Windows Vista 和更新版本上，Winsock 核心 (WSK) ，這是一個新的傳輸獨立核心模式網路程式設計介面，可用來撰寫網路通訊協定驅動程式。</span><span class="sxs-lookup"><span data-stu-id="24e72-197">On Windows Vista and later, Winsock Kernel (WSK), a new transport-independent kernel mode Network Programming Interface can be used to write a network protocol driver.</span></span> <span data-ttu-id="24e72-198">在 Windows Server 2003 及更早版本上，可以撰寫傳輸驅動程式介面 (TDI) 提供者和 Winsock helper DLL，以支援網路通訊協定。</span><span class="sxs-lookup"><span data-stu-id="24e72-198">On Windows Server 2003 and earlier, a Transport Driver Interface (TDI) provider and a Winsock helper DLL can be written to support the network protocol.</span></span> <span data-ttu-id="24e72-199">接著會將網路通訊協定新增至 Winsock 目錄作為支援的通訊協定。</span><span class="sxs-lookup"><span data-stu-id="24e72-199">The network protocol would then be added to the Winsock catalog as a supported protocol.</span></span> <span data-ttu-id="24e72-200">這可讓多個應用程式開啟此特定通訊協定的通訊端，而設備磁碟機可以追蹤哪個通訊端接收特定的封包和錯誤。</span><span class="sxs-lookup"><span data-stu-id="24e72-200">This allows multiple applications to open sockets for this specific protocol and the device driver can keep track of which socket receives specific packets and errors.</span></span> <span data-ttu-id="24e72-201">如需有關撰寫網路通訊協定提供者的詳細資訊，請參閱 Windows 驅動程式套件 (WDK) 的 WSK 和 TDI 章節。</span><span class="sxs-lookup"><span data-stu-id="24e72-201">For information on writing a network protocol provider, see the sections on WSK and TDI in the Windows Driver Kit (WDK).</span></span>

<span data-ttu-id="24e72-202">應用程式也必須留意防火牆設定可能對使用原始通訊端傳送和接收封包造成的影響。</span><span class="sxs-lookup"><span data-stu-id="24e72-202">Applications also need to be aware of the impact that firewall settings may have on sending and receiving packets using raw sockets.</span></span>

 

 
